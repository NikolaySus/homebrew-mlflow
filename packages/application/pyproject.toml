[project]
name = "homebrew-mlflow-application"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["homebrew-mlflow-domain"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/homebrew_mlflow"]
