global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: homebrew-mlflow-api
    static_configs:
      - targets: ["api:8000"]
